/**
 * Garbage Collection is called GC, and it is woefully out of date and should be ignored completely.
 * But we should inform visitors to know about GC is working in the background of JavaScript.
 * Even if we don't know about GC, but developers are using JavaScript.
 * If we know well about GC, then we can write JavaScript code better.
 */